let
    a = 11
    b = 4

echo "a + b = ", a + b      # Sum
echo "a - b = ", a - b      # Substraction
echo "a * b = ", a * b      # Multiplication
echo "a / b = ", a / b      # Division with full float number
echo "a div b = ", a div b  # Division with only integer part
echo "a mod b = ", a mod b  # Division integer remainder

